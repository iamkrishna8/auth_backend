# Backend

Minimal Express backend for the Authentication project.

Commands:

- Install dependencies:

```
cd backend
npm install
```

- Run in development (with nodemon):

```
npm run dev
```

Environment:

- Create a `.env` file (not committed) with `PORT` and `MONGO_URI`.
